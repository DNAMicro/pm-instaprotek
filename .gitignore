# Credentials — never commit
credentials.json

# Playwright session storage
.claude/skills/instaprotek-enterprise-registration/.auth/

# Cached CRM brand menu (stale-able, re-readable)
.claude/skills/instaprotek-enterprise-registration/config/brand_menu_cache.json

# Run artifacts
processed/**/screenshots/
processed/**/run_log.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS / IDE
.DS_Store
.idea/
.vscode/

# LibreOffice / Excel lock files
.~lock.*#

# Playwright install artifacts
node_modules/

# Playwright session state (contains live login tokens)
auth_state.json
**/auth_state.json

# Playwright MCP page snapshots/screenshots (may contain customer PII)
.playwright-mcp/

# Regression run evidence screenshots (may contain customer PII —
# the nullnet env carries live production data, see run-2026-08-17)
tmp/**/evidence/*.png
